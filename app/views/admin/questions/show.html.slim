p Вопрос #{@question.body} теста #{@question.test.title}

p = link_to 'Edit', edit_admin_question_path(test_id: @question.test.id, id: @question.id)
p = button_to 'Delete', admin_question_path(@question.id), method: :delete, data: { confirm: 'Вы уверены, что хотите удалить этот вопрос?' }
p = link_to 'Back', admin_test_path(@question.test)

p Ответы

- @question.answers.each do |answer|
  p = link_to answer.body, admin_answer_path(answer.id)

p = button_to "Создать ответ", new_admin_question_answer_path(question_id: @question.id), method: :get

