h3 Тест #{@test.title}

- @test.questions.each do |question|
  p = link_to question.body, question_path(test_id: @test.id, id: question.id)

p = button_to "Создать вопрос", new_test_question_path(test_id: @test.id), method: :get

p = link_to 'Back', tests_path

